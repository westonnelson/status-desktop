import libstatus/accounts/constants

export DATADIR
export STATUSGODIR
export KEYSTOREDIR
export TMPDIR


# TODO: Update this constant once the script for uploading newer versions into IPFS is complete
#       and an ENS is available in all networks

const APP_UPDATES_ENS* = "testupdates.eth" 